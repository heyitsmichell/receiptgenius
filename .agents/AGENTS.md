# Git & Deployment Rules

- **Commit & Update Messages**: Do NOT use conventional commit prefixes (`feat:`, `fix:`, `chore:`, `refactor:`, etc.) when writing git commit messages or `eas update` / deployment messages. Write concise, descriptive summary sentences directly without prefixes (e.g., "Add custom date range inputs to spending history and local export").
